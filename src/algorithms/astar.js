export function astar(grid, startNode, finishNode) {
    const openSet = []
    const closedSet = []
    startNode.distance = 0;
    openSet.push(startNode)


    while (
        openSet.length > 0
    )
}